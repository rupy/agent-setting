# Development workflow

- ファイル検索には、利用可能なら `rg` と `rg --files` を優先する。
- リポジトリ内の README、CONTRIBUTING、設定ファイル、近接する実装を先に確認する。
- バグ修正では症状だけでなく原因を特定し、可能なら再発を検出するテストを追加する。
- フォーマッターや自動修正を実行するときは、対象を依頼範囲に限定する。
- 作業終了前に `git diff` を確認し、意図しない変更やデバッグ出力が残っていないことを確かめる。

